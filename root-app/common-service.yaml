apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: common-service-bootstrap
  namespace: argocd
spec:
  destination:
    namespace: shared-service
    name: in-cluster
  source:
    path: common-service
    repoURL: "https://github.com/Sotatek-HuyNguyen17/ArgoCD-App-Of-App-Pattern"
    targetRevision: HEAD #latest revision on the branch
    directory:
      recurse: true
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
      - PruneLast=true
