apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-ingress
  namespace: my-app-prod
spec:
  ingressClassName: nginx
  rules:
  - host: my-app-prod.demo.kubeadm
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: web-svc   
            port:
              number: 80